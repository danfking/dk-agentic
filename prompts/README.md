# Prompts

Reusable prompt templates. Plain markdown, no agent-specific format.

Empty for now. As I notice myself reaching for the same multi-paragraph prompt across sessions, it lands here. Each prompt gets its own `.md` file with a short header explaining when to use it.
