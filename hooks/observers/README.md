# Observers

Record what happened. Never block. See `../README.md` for the full contract.

Empty for now. As I find specific things worth measuring (tool-call rates, time-in-edit, retries), they land here. Each observer should write to an append-only log under a known location and exit cleanly even on disk-full.
