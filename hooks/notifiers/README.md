# Notifiers

Side-effectful nudges: chimes, push notifications, Slack pings. Best-effort, never block. See `../README.md` for the full contract.

Empty for now. The chime hook from my blog post lives in user-level settings rather than here, since it's machine-specific. If I write a portable equivalent, it lands here.
